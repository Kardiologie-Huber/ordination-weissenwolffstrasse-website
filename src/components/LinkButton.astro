---
const { text, href, target, color, style } = Astro.props;
---

<p class="text-center">
  <a
    class={'btn btn-' + (color ? color : 'primary')}
    aria-current="page"
    role="button"
    href={href}
    style={style}
    target={target ?? '_self'}>{text}</a
  >
</p>
